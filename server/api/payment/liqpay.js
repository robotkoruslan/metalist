'use strict';

var express = require('express');
var controller = require('./liqpay.controller');

var router = express.Router();

// router.post('/callback', controller.callback);

module.exports = router;
